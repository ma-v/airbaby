<div class="container">
  <div class="row justify-center">
    <h1>My Babies</h1>
    <div class="row">
      <ul>
        <% current_user.babies.each do |baby| %>
        <%= link_to(baby_path(baby)) do %>
        <div class="card-product mt-4">
          <%= cl_image_tag baby.photo, height: 180, width: 180, crop: :fill %>
          <div class="card-product-infos">
            <h2><%= baby.title %></h2>
            <h3><%= baby.first_name %></h3>
            <p><%= baby.description %></p>
            <h3><%= baby.price_per_day %> € / per night</h3>
          </div>
          <!-- <div class="card-product"> -->
            <%# link_to "See my baby", baby_path(baby), class: "dropdown-item" %>
            <%# link_to "Delete", destroy_baby_path(baby), method: :delete, class: "dropdown-item" %>
          <!-- </div> -->
        </div>
          <% end %>
        <% end %>
      </ul>

    </div>

  </div>
</div>
